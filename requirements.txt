# ---------------------------------------------------------------------------
# Python requirements for ee-wunder-devtools-ubi9
#
# Purpose:
# - Run collection CI tooling inside a container:
#   ansible-lint, yamllint, molecule (+ docker driver), plus optional helpers.
# - Ansible is pinned to 2.18.x (collection baseline).
# - Guard non-breaking deps with upper bounds to avoid surprise major upgrades.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Core Ansible tooling (pinned for reproducibility)
# ---------------------------------------------------------------------------
ansible-core==2.18.19
ansible-lint==6.22.2
antsibull-changelog==0.35.1

# Version 50.0.0 fixes CVE-2026-69247 in PKCS#7 EnvelopedData decryption.
cryptography==50.0.0

# ---------------------------------------------------------------------------
# YAML linting (allow patch/minor, forbid major)
# ---------------------------------------------------------------------------
pyyaml==6.0.3
yamllint>=1.35.0,<2.0.0
pre-commit==4.6.2
mypy==2.3.1
ruff==0.16.4
types-PyYAML==6.0.12.20260815
uv==0.12.12

# ---------------------------------------------------------------------------
# Shell linting
# ---------------------------------------------------------------------------
shellcheck-py==0.11.0.1

# ---------------------------------------------------------------------------
# Molecule (pin to known-good versions on Python 3.11)
# - molecule-plugins contains drivers (incl. docker)
# ---------------------------------------------------------------------------
molecule==25.12.0
molecule-plugins==25.8.12

# ---------------------------------------------------------------------------
# Docker Python client stack
# - molecule docker driver + any docker API usage in helper scripts
# ---------------------------------------------------------------------------
docker==7.2.0
requests>=2.31.0,<3.0.0
requests-unixsocket>=0.3.0,<1.0.0

# ---------------------------------------------------------------------------
# VMware SDK (only needed if devtools runs VMware modules/tests)
# - community.vmware / vmware.vmware expect pyvmomi (provides pyVim)
# ---------------------------------------------------------------------------
pyvmomi==9.1.0.0

# ---------------------------------------------------------------------------
# COPR CLI (needed to configure/build COPR packages from inside the container)
# ---------------------------------------------------------------------------
copr-cli==2.6
