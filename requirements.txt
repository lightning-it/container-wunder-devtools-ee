# Core Ansible tooling
ansible-core==2.15.13
ansible-lint==6.22.2
yamllint>=1.35.0

# Molecule + plugins (without extras so we can pin docker ourselves)
molecule==6.0.3
molecule-plugins==23.5.3

# Docker Python client (used by community.docker, Molecule docker driver, etc.)
docker==6.1.3

# Workaround for Docker SDK + requests "http+docker" bug:
# requests 2.32.x can cause "Not supported URL scheme http+docker"
# → explicitly pin to 2.31.0 for stable behavior.
requests==2.32.4

# Ansible community.vmware collection dependency
pyvmomi==9.0.0.0
